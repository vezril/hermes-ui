import { Playground } from "@/components/hermes/playground";

/** Publish view — publish a message to a topic and watch a live tap of its traffic. */
export default function PublishPage() {
  return <Playground />;
}
