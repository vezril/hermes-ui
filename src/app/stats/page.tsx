import { Statistics } from "@/components/hermes/statistics";

/** Statistics view — broker-wide totals plus per-topic and per-subscription breakdowns. */
export default function StatsPage() {
  return <Statistics />;
}
