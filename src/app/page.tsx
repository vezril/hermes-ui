import { TopicManager } from "@/components/hermes/topic-manager";

/** Topics view — create, edit labels, delete, and list all HermesMQ topics. */
export default function TopicsPage() {
  return <TopicManager />;
}
